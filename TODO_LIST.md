# TODO_LIST

Short- and mid-term improvement tasks. Done work is deleted, never struck
through (docs-health style: one home per fact, no decay). Long-shot ideas
live in FEATURES.md as WORTH_CONSIDERING.

| Task                                                                                                                                                                         | Status    | Priority | Effort | Evidence / notes                                                                                            |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | -------- | ------ | ----------------------------------------------------------------------------------------------------------- |
| Push the accept/reject fix (`00f13fe`) and bump the telephony stack's `webphone` input lock to it (currently pinned to the pre-fix `276c596`)                                | 🔴 `TODO` | High     | S      | Browser E2E green with the fix (local override run 2026-09-18); stack lock bump is one command after push   |
| Tag v2.0.0 on GitHub so CHANGELOG compare/release links resolve (lychee 404 × 2)                                                                                             | 🔴 `TODO` | Medium   | S      | `CHANGELOG.md`; buildflow lychee findings 2026-09-18                                                        |
| Track glibc CVE-2026-5450 (9.8) until nixpkgs ships a fix — nixpkgs bumped to 2026-09-17 and it is still unfixed; runtime closure is otherwise clean (8 derivations, vulnix) | 🔴 `TODO` | Medium   | S      | vulnix runtime-closure scan 2026-09-18; build-time-only advisories (binutils/gcc/bootstrap Go) never deploy |
| Audit-integrity closure: read the never-read server files (`webhooks.go` bodies, `actions.go` tail, `panels.go`, `proxy.go`, `configjs.go`, `assets.go`, `unread.go`), finish the cqrs-htmx symbol map (ack/notify/decoder/partial/redirect/security/openapi/event-catalog), add the rubric + effort columns to the deep-dive report, re-score adoption (~90 target), ANNOTATE the 2026-09-18 reports (plan M20-M28) | 🔴 `TODO` | High     | M      | `docs/planning/2026-09-18_21-45_cqrs-htmx-adoption-pareto-execution-plan.md` §Phase 4; audit cites master not the v4.9.0 tag for two claims |
| Rate-limit `GET /events` with the KeyedRateLimiter (SSE reconnect churn is unthrottled today; login/hooks pattern already proven) (plan EL1/M35)                              | 🔴 `TODO` | High     | S      | plan §Phase 5; `internal/server/server.go` open mux; limiter wiring pattern at `POST /api/session`          |
| Island: listen for the SSE `connected` event → "live" indicator (same-origin asset edit, no ids touched) + DOM-contract assertion (plan CO1-CO2/M31-M32)                      | 🔴 `TODO` | Medium   | S      | plan §Phase 5; frame byte-verified by `TestSSEStreamCarriesConnectedThenEvents`                             |
| Hub idle teardown: TTL reaper for `ExtensionHubs` (hubs are created lazily and never removed) + no-broadcast-after-close test (plan HT1-HT2/M36-M37)                          | 🔴 `TODO` | Medium   | M      | plan §Phase 5; `internal/server/sse.go` `ExtensionHubs`                                                     |
| Webhook idempotency: dedupe replayed `provider_ref` in `/hooks/*/status` (store choice memory-vs-SQL) + replay test + AGENTS.md note (plan ID1-ID3/M38-M39)                   | 🔴 `TODO` | Medium   | M      | plan §Phase 5; `internal/server/webhooks.go`                                                                |
