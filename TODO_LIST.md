# TODO_LIST

Short- and mid-term improvement tasks. Done work is deleted, never struck
through (docs-health style: one home per fact, no decay). Long-shot ideas
live in ROADMAP.md (raw ideas + open questions); owner calls there too.

| Task                                                                                                                                                                                                                                                                                                                                                                                 | Status       | Priority | Effort | Evidence / notes                                                                                                     |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------ | -------- | ------ | -------------------------------------------------------------------------------------------------------------------- |
| Redeploy production with webphone v2.1.0 (owner ssh): `cd ~/projects/pbx-artmann && nixos-rebuild test --flake .#pbx --target-host root@pbx.artmann.tech`, verify `/version` = v2.1.0, then `switch` — URGENT: the deployed v2.0.0-era build 403s every browser login behind the TLS proxy (csrf attestation bug, fixed in v2.1.0)                                                   | 🔴 `TODO`    | High     | S      | lock chain re-pinned `019093f` (pbx-artmann) → stack `f8eb226` → webphone `d815004`; pre-build green; SUPERB plan P1 |
| Owner decisions (memos with recommendations in the SUPERB plan P8): stack pin policy (ride main vs release tags) and pbx-artmann input type (`path:` vs github pin)                                                                                                                                                                                                                  | 🔴 `TODO`    | Medium   | S      | 11:02 report §g.1/§f.27/§f.28; AGENTS `DECIDED` lines on answer                                                      |
| Debug the 1001-registration anomaly in the stack browser E2E (ext 1001 registration vanished after an nginx restart in the round-1 run, `sofia_contact` → `error/user_not_registered`; never root-caused, round 2 passed): instrument the reconnect phase with a sofia registration dump, read the island reload-fallback path, fix island or E2E per findings, verify ×2 green runs | 🟡 `PLANNED` | High     | M      | SUPERB plan P5; round-1 anomaly 2026-09-19                                                                           |
| Version-drift guard: `flake.nix webphoneVersion` must equal the newest tag (buildflow step or Go test)                                                                                                                                                                                                                                                                               | 🟡 `PLANNED` | Medium   | S      | SUPERB plan P13; f.11 class                                                                                          |
| Island sanitization alignment (owner decision): Go `sanitizeDialable` keeps letters (alphanumeric SIP user parts) but the island's dial/login regex `/[^\d+*#]/g` strips them — the "mirrors exactly" claim was false and is now corrected; pick a side (extend the island regex to keep letters, or drop letters server-side) and align both + the pinning test | 🟡 `PLANNED` | Low      | S      | found by the 2026-09-19 skills sweep (full-code-review/data-model); island change is gated on the stack browser E2E |
| Release runbook script (`release.sh`): clean-tree check → fold-check → bump → gates → tag → push → lychee → stack lock → stack gates → aarch64, with dry-run idempotence                                                                                                                                                                                                             | 🟡 `PLANNED` | Medium   | L      | SUPERB plan P14                                                                                                      |
| Release ops for v2.1.0: `gh release create` (CHANGELOG excerpt), CHANGELOG link refs, announcement draft (owner approves posting)                                                                                                                                                                                                                                                    | 🟡 `PLANNED` | Low      | S      | SUPERB plan P15; ROADMAP open question "Release objects"                                                             |
| Backup/restore story for `/var/lib/webphone`: inventory (SQLite + blobs), documented rsync/restic pattern, restore drill on scratch, module timer skeleton                                                                                                                                                                                                                           | 🟡 `PLANNED` | Medium   | L      | SUPERB plan P17                                                                                                      |
| Island adoption fallback: retry ×3 with backoff before the reload fallback in `session.js`, documented tradeoff (keeps SIP registration in the common case)                                                                                                                                                                                                                          | 🟡 `PLANNED` | Low      | M      | SUPERB plan P19                                                                                                      |
| Nix surface: typed module options for `csrf.trusted_origins`/`trusted_proxies` (defaults preserved) + stack-side assertion of its rendered `settings.csrf`                                                                                                                                                                                                                           | 🟡 `PLANNED` | Low      | S      | SUPERB plan P20                                                                                                      |
| CSRF rotation on session TTL refresh (spec + implement + tests; island adoption path already handles re-keying)                                                                                                                                                                                                                                                                      | 🟡 `PLANNED` | Low      | L      | SUPERB plan P23                                                                                                      |
| Inbound fax feed: convert the stack's rxfax TIFFs to PDF → webphone `/hooks/fax` once messaging is live (locate rxfax, convert step behind a toggle, secret wiring, loopback test)                                                                                                                                                                                                   | 🔴 `TODO`    | Low      | L      | SUPERB plan P24; 11:02 report f.22-f.24 shipped the SMS lane                                                         |
| Idiomorph swap experiment for SSE/HTMX partials on an experiment branch, verdict doc, gated on the stack's browser E2E                                                                                                                                                                                                                                                               | 🟡 `PLANNED` | Low      | M      | SUPERB plan P25; cqrs-htmx superb-adoption plan (open item)                                                          |
| Standing watches: sip.js 0.22 / templ-components ThemeScript opt-out / oxlint globals / E2E wall-time budget (151s baseline) + FEATURES VERIFY pass + v2.1.1 hotfix pre-draft + nanoid ≥ v1.65.1 (blocked: requires Go ≥ 1.27; repo pins GOTOOLCHAIN=local + cqrs-htmx floor go1.26.7)                                                                                              | 🟡 `PLANNED` | Low      | M      | SUPERB plan P27; nanoid watch added by the 2026-09-19 skills sweep                                                   |
| Liveness/watchdog decision (owner call): `/healthz` is readiness-only — systemd `Restart = on-failure` covers crashes but nothing detects a hung process; pick document-only vs `WatchdogSec` + sd_notify heartbeat vs stack-side gating on `/healthz`, then document the choice (README + module comment)                                                                            | 🔴 `TODO`    | Medium   | S      | DI/health review 2026-09-19 F2 (`docs/architecture-understanding/2026-09-19_18-49_samber-do-di-health-service-orientation.md`) |
| Readiness check timeout guard: wrap the `/healthz` named checks (`sqlite`, `blob-dir`) in a bounded-timeout helper (503 names `<check>: timed out`) + test; optionally propose a per-check timeout upstream to cqrs-htmx `ReadinessHandler`                                                                                                                                          | 🟡 `PLANNED` | Medium   | S      | DI/health review 2026-09-19 F1; both checks local today, the guard matters once a networked check lands              |

