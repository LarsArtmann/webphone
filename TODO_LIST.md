# TODO_LIST

Short- and mid-term improvement tasks. Done work is deleted, never struck
through (docs-health style: one home per fact, no decay). Long-shot ideas
live in ROADMAP.md (raw ideas + open questions); owner calls there too.

Last sweep: 2026-09-22 evening (docs-health AUDIT over all `**/2026-0*`
docs; harvested the 2026-09-22 status reports). Closed since the previous
sweep: the 1001-registration anomaly (root-caused + fixed + E2E green ×2),
the E2E redial stall (stack `5425e6f` + island `5184471` — not a flake),
the FIRE gate (erraudit tier-1 = 0, `4320c7d`), the dedup train (zero
harmful clones at `-t 3`), the ThemeScript/CSP train (zero inline scripts),
the prod-reality correction (prod serves v2.4.0, bogus credentials
rejected — no emergency), and every T-item of the 20-year plan except the
rows below.

| Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Status       | Priority | Effort | Evidence / notes                                                                                                                                 |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | -------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Push webphone `main` — the daemon's push has STALLED: origin/main sits at `5cce98d` (17:53 era) while local HEAD is 40 commits ahead (dedup train, nginx.gzip + /metrics, signed tags, CRM erraudit fixes, the whole ThemeScript/CSP train, docs). Every tri-repo step (stack re-pin, pbx-artmann relock) is blocked on this. Check the pusher daemon first, then push; verify with `git ls-remote`, not push logs                                                                                                                                                                                                                                                                                          | 🔴 `TODO`    | High     | S      | `git ls-remote origin main` vs `git rev-parse HEAD`, 2026-09-22 evening                                                                          |
| Cut + ship the next release train: [Unreleased] holds three coherent themes (every-surface-answers-back T20/T21/T25/T26/T27, Ledger CRM, ThemeScript/CSP zero-inline) — fold, bump, gates, tag (release.sh now signs tags, `4f067f8`). INCLUDES the twice-skipped declared markup gate: stack browser E2E re-run (composer + theme trains changed served markup/JS; budget 445s), aarch64 cross-build + ELF `b700` guard, smoke `--expect-version`, then stack re-pin + pbx-artmann relock #4                                                                                                                                                                                                               | 🔴 `TODO`    | High     | M      | `docs/status/2026-09-22_18-55_*` §b2/b3/c2; `docs/status/2026-09-22_18-46_*` f9; docs/release-runbook.md                                         |
| Deploy the released chain to prod (OWNER terminal — pbx-artmann AGENTS forbids assistant ssh/deploy): prod serves v2.4.0 today (smoke 16/0, bogus rejected). Owner decision folded in: deploy the v2.5.0 tag now vs fold the current [Unreleased] into the next train first and deploy once (asked 18:46 g3). COMMAND: `cd ~/projects/pbx-artmann && nixos-rebuild test --flake .#pbx --target-host root@pbx.artmann.tech` → smoke → `nixos-rebuild switch`                                                                                                                                                                                                                                                 | 🔴 `TODO`    | High     | S      | v2.5.0 tag `25740c6` released 2026-09-22, release EXIT=0, both pbx-artmann toplevels green on the chain                                          |
| OWNER post-deploy verification: after the switch run `python3 scripts/webphone-smoke.py --base https://pbx.artmann.tech --expect-version <deployed>` (0 failed) and the rejection-banner check — sign in, self-send an SMS to the PBX DID, expect the persisted provider reason in the failed bubble (needs a real extension session; anonymous probes cannot cover it)                                                                                                                                                                                                                                                                                                                                     | 🔴 `TODO`    | High     | S      | error-excellence + failed-bubble trains shipped in 2.5.0/Unreleased; `--expect-version` verified positive + negative                             |
| vulnix NVD feed 404 breaks the release gate (found 2026-09-22 ~18:15): vulnix 1.12.5 fetches the retired NVD JSON 2.0 modified feed, which answers 404 — `nix run .#vulnix` crashes WITHOUT scan results (retried twice). `scripts/release.sh` gates on this step, so the next release is BLOCKED until fixed. Fix directions: bump/patch vulnix upstream (nixpkgs), repoint the wrapper at a working mirror, or swap the gate to govulncheck (already runs in buildflow). OWNER/triage call needed                                                                                                                                                                                                         | 🔴 `TODO`    | High     | M      | buildflow 2026-09-22: 51/52 steps green, only vulnix failed                                                                                      |
| Outbound SMS bridge failure on prod: `POST /messages/send` answered 422 (gateway error) when probed 2026-09-19 — webphone-side classification is 502 + provider-rejection detail since `1d53f44` (rides the next deploy), but the ROOT cause is stack-side (telnyx-webhooks bridge down/rejecting or Telnyx creds). OWNER (assistant ssh is forbidden by pbx-artmann AGENTS): journal the telnyx-webhooks unit for today, grep for sms/422/error lines, restart or fix creds per findings, send a test SMS; record the root cause here + in the stack runbook                                                                                                                                               | 🔴 `TODO`    | High     | S      | 2026-09-19 probe; webphone-side fix `1d53f44`                                                                                                    |
| Theme-knob session's admitted gaps (2026-09-22 18:55, all small): (a) add `internal/web/assets/theme-preload.js` to the `island-lint` oxlint invocation — the no-undef fail-closed gate does NOT cover the new root-level asset file; (b) formal node:test spec for theme-preload.js in `island-tests/` (today's proof was an ad-hoc eval); (c) smoke check: `/assets/theme-preload.js` answers 200 + `application/javascript` and served `/` carries zero inline `<script>`                                                                                                                                                                                                                                | 🟡 `PLANNED` | Medium   | S      | `docs/status/2026-09-22_18-55_*` §d6/§f2-4; oxlint scope verified 2026-09-22 evening                                                             |
| Full gates on the converged tree: `BUILDFLOW_NO_RESULT_CACHE=1 buildflow` (gitleaks, codespell, markdown-lint, branching-flow — not run since the trains landed) + repo-wide erraudit tier-1 re-run + tier-2 recount (bar: 102 stdlib-constructor findings, must SHRINK; scheduled measure 2026-10-22 — do it early, three error-path trains landed since)                                                                                                                                                                                                                                                                                                                                                  | 🟡 `PLANNED` | Medium   | M      | `docs/status/2026-09-22_18-55_*` §b4; AGENTS erraudit bar; the CRM session's 7 findings fixed at `4320c7d`                                       |
| Theme-knob session's remaining verification: aarch64 cross-build (release-time, but cheap insurance pre-fold), FOUC measurement (one headless first-paint screenshot pair converts the pre-paint `data-theme` argument from mechanism to evidence), German-native review of the new copy, screenshot QA of the four composer affordances                                                                                                                                                                                                                                                                                                                                                                    | 🟡 `PLANNED` | Medium   | S      | `docs/status/2026-09-22_18-55_*` §b4/§c4; 17:53 §b3                                                                                              |
| Analyze the next E2E flake with the shipped `transfer_dbg()` timestamped dumps instead of re-instrumenting. RESOLVED-IN-PART 2026-09-22: the post-transfer-redial stall class was NOT a flake — `dial_into_call` appended into a never-cleared #dest and dialed "10011001" (stack fix `5425e6f`, island `placeCall` verdict `5184471`); the remaining transfer-step channel-count death stays the documented re-run-once mode, and the REGISTRATIONS-0 secondary wedge is still unexplained — dumps are the first stop                                                                                                                                                                                      | 🟡 `PLANNED` | Medium   | S      | two stalled runs + the redial dump naming 10011001; 18:46 §b1 (run 3 sailed the previously-stalled section)                                      |
| Send-failure UX follow-ups (A+B+D shipped; D = the failed-bubble story `1bec154`): (C) pre-flight self-send 422 fast path — OWNER call: instant refusal vs today's evidence-preserving failed row; (E) provider refusal → 422 + honest log family (test + failure→feedback table + stack runbook must move together) — the last missing piece; (F) own-DID on the session payload + live composer warning — only if self-sends recur after B(+C); plus the fax-lane self-send guard (rides C)                                                                                                                                                                                                               | 🟡 `PLANNED` | Medium   | S–L    | plan `docs/planning/2026-09-22_16-07_SUPERB-send-failure-ux.md`; live 40310 self-send session 2026-09-22                                         |
| CRM integration follow-ups (both sides green + documented): (a) island unit test for `recordCrmCall`; (b) idempotency key on `POST /api/calls` (an island retry could double-journal); (c) single-flight in `crm.Resolver` (a 30-row history cache-miss burst fans out one lookup per number); (d) lookup hit/miss/timeout counters; (e) stack-side wiring for `crm.url`/`crm.token` (secrets dir); (f) cross-doc the CRM surfaces into the stack runbook; (g) restore-drill: prove `call_logged` entries survive a CRM journal restore; (h) one HTTP-level island→server→CRM-stub integration test                                                                                                         | 🟡 `PLANNED` | Medium   | S–M    | `docs/status/2026-09-22_16-54_crm-integration-train-status.md` §f items 10-19; integration shipped behind `crm.url`+`crm.token` (OFF by default) |
| Dedup-train contract pins (2026-09-22 17:40): dedicated tests for the NEW extractions — `listRows` wraps an iteration error with the `op` shape; `requireMultipartTo` answers 422 with the per-tab key; nil-client `pbx.do()` disabled short-circuit; owner-scoping coverage over the NEW helper path. Then: crm/client.go disabled-policy — same `do()` chokepoint as pbx or document the divergence (split brain today)                                                                                                                                                                                                                                                                                   | 🟡 `PLANNED` | Medium   | S–M    | `docs/status/2026-09-22_17-40_dedup-train-status.md` §c2/c3, §e5; art-dupl verdict: zero harmful clones at `-t 3`                                |
| OWNER-calls batch session (~15 decisions, one sitting): train-cut/cadence, `/livez` consumer, HSTS, XFF sanitization, disclosure posture, loopback `delivered` semantics, handler dual-layer, gh-release habit, Go module v2 policy, recordings intent, TEMP-DIAG keep, oops ratification, `backup.retentionDays`, THEN the three CRM policy calls (typed NixOS `crm.{url,token}` options vs freeform-secret; multi-contact "+N more"; English-only journal bodies), the self-send train-C semantics, the Tailwind coexistence spike, and the templ-components history-blemish disposition — briefing at `docs/planning/2026-09-22_13-50_owner-calls-briefing.md`                                           | 🟡 `PLANNED` | High     | S      | ROADMAP "Open questions" + TODO owner rows consolidated; new asks from the 16:54/17:53/18:46/18:55 §g sections                                   |
| T26b TURN REST credentials, BOTH halves: webphone half (`turn_rest_secret` config + TTL + `/config.js` emission + test) and stack half (coturn `static-auth-secret` + REST API with the SAME shared secret)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 🟡 `PLANNED` | Low      | M      | plan T26b (`docs/planning/2026-09-22_12-02_SUPERB-20-year-durability.md`); stack row parked at `34e741d`                                         |
| Post the release announcements (drafts for v2.1.0–v2.3.0 at `docs/announcements/2026-09-20_v2-1-0_v2-2-0_v2-3-0_drafts.md`, v2.4.0 one-liner folded there, v2.5.0 headline + one-liner at `docs/announcements/2026-09-22_v2-5-0_drafts.md`; owner picks channel(s), approves wording, decides the disclosure posture — fix-acknowledged, no exploit detail)                                                                                                                                                                                                                                                                                                                                                 | 🟡 `PLANNED` | Low      | S      | all release objects live through v2.5.0                                                                                                          |
| OWNER: ratify the oops non-fix (T15): samber/oops stays a documented non-fix (AGENTS erraudit bar tier 3) or order a staged-adoption plan doc — do not improvise the refactor either way                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 🟡 `PLANNED` | Low      | S      | plan D2; 2026-09-21 triage: oops is not a dependency, families adopted instead                                                                   |
| Standing watches — QUARTERLY RE-CHECK, next due 2026-12-20 (named triggers fire earlier): sip.js 0.22 (swap ONLY on Chromium WebRTC breakage, a sip.js security advisory, or a needed capability — re-checked 2026-09-22: npm latest is still 0.21.2) · templ-components upstream v1.20.x watch (ThemeScript-adjacent cleanups, chart family, DataTable maturity; keep the AGENTS adoption table honest per adoption) · oxlint globals (any new browser global → `internal/web/assets/island/oxlint.json`; theme-preload.js scope gap tracked above) · E2E wall-time budget 445s (two forced-rebuild runs 384s/373s on the v2.5.0 chain) — watch TWO consecutive over-budget runs · nanoid CLOSED (v1.65.1) | 🟡 `PLANNED` | Low      | S      | T20; triggers re-verified 2026-09-22; `docs/status/2026-09-22_18-55_*` §f17                                                                      |
