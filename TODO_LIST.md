# TODO_LIST

Short- and mid-term improvement tasks. Done work is deleted, never struck
through (docs-health style: one home per fact, no decay). Long-shot ideas
live in ROADMAP.md (raw ideas + open questions); owner calls there too.

Last sweep: 2026-09-23 (docs-health AUDIT over the v2.6.0 state).
Closed since the 2026-09-22 night sweep: the v2.6.0 train itself —
fold, gates, signed tag `807ca0c` pushed to origin; all four
dedup-train contract pins + the crm `do()` chokepoint + resolver
single-flight/counters + `/api/calls` UUID idempotency
(`TestAPICallLoggingContract` ×4 under `-race`); the CRM metrics
family; thread search + audio picker + hover stamps + jump chip +
missed-call badge + dial typeahead (the full ux-raw-ideas train);
the `-t 1` and `-t 3` art-dupl trains (13+5 groups triaged, 10
one-home helpers extracted, zero harmful clones at `-t 3`); the
theme-preload verification gaps (island-lint scope, node spec, smoke
4a); buildflow full RC 0 + `nix flake check` all-green on the tagged
tree; lychee broken-link fix (`7503561`).

| Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Status       | Priority | Effort | Evidence / notes                                                                                                                                 |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | -------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Finish the v2.6.0 release TAIL (tag `807ca0c` is pushed + gate-verified; two E2E stalls were load-shaped, the armed chained retry NEVER fired — `/tmp/release26d.log` is gone, no gh release object exists): re-run release.sh steps 6–9 on a quiet host (lychee → stack relock verify against current main → stack browser E2E green → aarch64 ELF `b7 00` re-verify → `gh release create` with the CHANGELOG body); then `python3 scripts/webphone-smoke.py --expect-version 2.6.0`; then pbx-artmann relock #4 (rev swap → `nix flake update telephony` → lock-drift-probe → both toplevels → ExecStart store-path moved → narrative commit + push); closing sweep `git ls-remote` per runbook step 9 | 🔴 `TODO`    | High     | M      | `docs/status/2026-09-23_02-47_v26-tagged_*` §b/c/f; stack currently pins `7503561`; verified 2026-09-23: no gh release, log gone, origin == HEAD |
| Deploy the released chain to prod (OWNER terminal — pbx-artmann AGENTS forbids assistant ssh/deploy): prod serves v2.4.0 today. Deploy once after the v2.6.0 tail lands (the old "v2.5.0 now vs fold-first" question is moot — v2.6.0 is tagged). COMMAND: `cd ~/projects/pbx-artmann && nixos-rebuild test --flake .#pbx --target-host root@pbx.artmann.tech` → smoke → `nixos-rebuild switch`                                                                                                                                                                                                                                                                                                                 | 🔴 `TODO`    | High     | S      | pbx-artmann relock #3 (`24cb90d`) pinned the v2.5.0-era chain; relock #4 rides the row above                                                        |
| OWNER post-deploy verification: after the switch run `python3 scripts/webphone-smoke.py --base https://pbx.artmann.tech --expect-version 2.6.0` (0 failed) and the rejection-banner check — sign in, self-send an SMS to the PBX DID, expect the persisted provider reason in the failed bubble (needs a real extension session; anonymous probes cannot cover it)                                                                                                                                                                                                                                                                                                                                           | 🔴 `TODO`    | High     | S      | failed-bubble train shipped in 2.6.0; `--expect-version` verified positive + negative                                                             |
| Outbound SMS bridge failure on prod: `POST /messages/send` answered 422 (gateway error) when probed 2026-09-19 — webphone-side classification is 502 + provider-rejection detail since `1d53f44` (rides the next deploy), but the ROOT cause is stack-side (telnyx-webhooks bridge down/rejecting or Telnyx creds). OWNER (assistant ssh is forbidden by pbx-artmann AGENTS): journal the telnyx-webhooks unit for today, grep for sms/422/error lines, restart or fix creds per findings, send a test SMS; record the root cause here + in the stack runbook                                                                                                                                               | 🔴 `TODO`    | High     | S      | 2026-09-19 probe; webphone-side fix `1d53f44`                                                                                                    |
| Micro-tests for the one-home helpers (both art-dupl trains landed them caller-pinned only — the avatarFor lesson): `crmNumbers` blank-skip, `domain.must`, `domain.OrClock`, `store.updatedOrNotFound`, `views.formatFor`, `applyStatusWebhook`, `recordCallIdem` empty-key, `contactSaveFailed`, `apiContactSaved` (notify→204 order + both JSON handlers route through it); plus an inventory of which tests cover `apiSaveContact`/`apiDeleteContact` with gap tests                                                                                                                                                                                                                                          | 🟡 `PLANNED` | Medium   | M      | `docs/status/2026-09-23_01-12_*` §f3, `docs/status/2026-09-23_03-01_*` §f1/f2/f6; helper bar itself = owner question (ROADMAP)                     |
| `go test -race ./internal/server/...` full-package pass on a quiet machine over the idem/hub paths the dedup trains refactored (`hooksIdem`, `callsIdem`, hub consumers); scoped `-race` ran green (crm tests) but the full package never did post-refactor                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 🟡 `PLANNED` | Medium   | S      | `docs/status/2026-09-23_01-12_*` §c2, `docs/status/2026-09-23_03-01_*` §f5                                                                       |
| Cross-check docs/error-contract.md against the consolidated webhook tail (`applyStatusWebhook`: validation precedence flipped to status-first; 400 texts byte-identical) and decide whether the JSON-mutations-answer-204 contract joins the table (keep both sides in sync rule)                                                                                                                                                                                                                                                                                                                                                                                                                             | 🟡 `PLANNED` | Medium   | S      | `docs/status/2026-09-23_01-12_*` §f11, `docs/status/2026-09-23_03-01_*` §f9/f10                                                                  |
| Release-runbook hardening once the owner picks the mechanism (02:47 §g1/g3): load precondition in release.sh (`/proc/loadavg` gate or single E2E auto-retry) + the flake heuristic one-liner ("same E2E step stalls twice = dig; different steps in the post-FS-restart tail = host load — wait for quiet") + release-attempt logging rule (always `> /tmp/release-<v>-<n>.log`, never pipe through tail) + one coordination-hazard note (v2.6.0 was cut while a refactor train was mid-flight)                                                                                                                                                                                                             | 🟡 `PLANNED` | Medium   | S      | `docs/status/2026-09-23_02-47_*` §e1/e2/e5, `docs/status/2026-09-23_01-12_*` §f13; mechanism = owner call                                        |
| golangci-lint LSP false positive: `export_test.go:30` carries a valid `//nolint:staticcheck,errcheck` and the CLI gate prints 0 issues, but the LSP keeps flagging it every session — fix the integration/config or declare the CLI gate the single lint truth in AGENTS (one line)                                                                                                                                                                                                                                                                                                                                                                                                                            | 🟡 `PLANNED` | Medium   | S      | `docs/status/2026-09-23_03-01_*` §e.2/§f11/§f26; full-tree `./internal/...` lint re-run rides this (03-01 covered server only)                      |
| Full-code-review pass over the interleaved 2026-09-22 day (CRM train + composer/UX trains + both dedup trains landed concurrently; no holistic review has run since)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 🟡 `PLANNED` | Medium   | M      | `docs/status/2026-09-23_01-12_*` §f10                                                                                                            |
| Theme-knob session's remaining verification: FOUC measurement (one headless first-paint screenshot pair converts the pre-paint `data-theme` argument from mechanism to evidence), German-native review of the new copy, screenshot QA of the four composer affordances (aarch64 cross-build already done — ELF `b7 00` verified)                                                                                                                                                                                                                                                                                                                                                                               | 🟡 `PLANNED` | Medium   | S      | `docs/status/2026-09-22_18-55_*` §b4/§c4; 17:53 §b3                                                                                              |
| Send-failure UX follow-ups (A+B+D shipped; D = the failed-bubble story `1bec154`): (C) pre-flight self-send 422 fast path — OWNER call: instant refusal vs today's evidence-preserving failed row; (E) provider refusal → 422 + honest log family (test + failure→feedback table + stack runbook must move together) — the last missing piece; (F) own-DID on the session payload + live composer warning — only if self-sends recur after B(+C); plus the fax-lane self-send guard (rides C); plus the stack-runbook CSP zero-inline cross-doc (rides E's runbook sync)                                                                                                                                 | 🟡 `PLANNED` | Medium   | S–L    | plan `docs/planning/2026-09-22_16-07_SUPERB-send-failure-ux.md`; live 40310 self-send session 2026-09-22; `docs/status/2026-09-22_18-55_*` §f31     |
| CRM integration follow-ups, stack-side remainder ((a)–(d) SHIPPED in the 21:12/02:47 trains — island test, idempotency, single-flight, counters): (e) stack-side wiring for `crm.url`/`crm.token` (secrets dir); (f) cross-doc the CRM surfaces into the stack runbook; (g) restore-drill: prove `call_logged` entries survive a CRM journal restore; (h) one HTTP-level island→server→CRM-stub integration test (or settle the level: contract test here vs stack E2E)                                                                                                                                                                                                                                       | 🟡 `PLANNED` | Medium   | S–M    | `docs/status/2026-09-22_16-54_*` §f 15-19; `docs/status/2026-09-23_02-47_*` §f5                                                                  |
| OWNER-calls batch session (decisions, one sitting — briefing at `docs/planning/2026-09-22_13-50_owner-calls-briefing.md`): the original ~15 (train-cut/cadence, `/livez` consumer, HSTS, XFF sanitization, disclosure posture, loopback `delivered` semantics, handler dual-layer, gh-release habit, Go module v2 policy, recordings intent, TEMP-DIAG keep, oops ratification, `backup.retentionDays`) + the three CRM policy calls (typed NixOS `crm.{url,token}` options; multi-contact "+N more"; English-only journal bodies) + self-send train-C semantics + Tailwind coexistence spike + templ-components history-blemish disposition + NEW from the v2.6.0 tail: release.sh load-gate vs E2E-retry (g1), stack pin-vs-tag for release trains (g2), host-contention/flock protocol (g3), art-dupl `-t 3` baseline ratification + suppression-bucket doc, webhook 400 body-text dependents + `msg/`→`message/` idem-key rename acceptance, helper micro-test bar (rule vs batched task), missed-call REJECT semantics, search `?q=` URL semantics, store `Must*` panic-on-corrupt policy | 🟡 `PLANNED` | High     | S      | ROADMAP "Open questions" carries the same list with context; questions route there, decisions land back here                                       |
| T26b TURN REST credentials — STACK half only: coturn `static-auth-secret` + REST API wired with the SAME shared secret as webphone's `turn_rest.secret` (webphone half SHIPPED: derivation + /config.js + boot validation + HMAC-verified tests, `c1971c4`)                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 🟡 `PLANNED` | Low      | M      | plan T26b (`docs/planning/2026-09-22_12-02_SUPERB-20-year-durability.md`); stack row parked at `34e741d`                                          |
| Post the release announcements (drafts for v2.1.0–v2.3.0 at `docs/announcements/2026-09-20_v2-1-0_v2-2-0_v2-3-0_drafts.md`, v2.5.0 at `docs/announcements/2026-09-22_v2-5-0_drafts.md`; v2.6.0 draft owed once the tail lands; owner picks channel(s), approves wording, decides the disclosure posture — fix-acknowledged, no exploit detail)                                                                                                                                                                                                                                                                                                                                                              | 🟡 `PLANNED` | Low      | S      | release objects live through v2.5.0; v2.6.0 gh object = tail row above                                                                           |
| Standing watches — QUARTERLY RE-CHECK, next due 2026-12-20 (named triggers fire earlier): sip.js 0.22 (swap ONLY on Chromium WebRTC breakage, a sip.js security advisory, or a needed capability — re-checked 2026-09-22: npm latest is still 0.21.2) · templ-components upstream v1.20.x watch (ThemeScript-adjacent cleanups, chart family, DataTable maturity; keep the AGENTS adoption table honest per adoption) · oxlint globals (any new browser global → `internal/web/assets/island/oxlint.json`) · E2E wall-time budget 445s — watch TWO consecutive over-budget runs · MONTHLY erraudit tier-2 re-measure next due 2026-10-22 (baseline 127 stdlib_constructor / 113 outside crm, must shrink) · nanoid CLOSED (v1.65.1)                                                    | 🟡 `PLANNED` | Low      | S      | AGENTS erraudit bar + `docs/status/2026-09-22_18-55_*` §f17; `docs/status/2026-09-23_03-01_*` §f23                                                |
