# Status report — v2.7.0 adoption run: M15–M22 complete, release tail hostage to a host-nix outage

Generated 2026-09-25 04:04 (Friday). Scope: THIS session's run — the
continuation of the SUPERB stack-adoption execution (M15 onward), the
v2.7.0 release train up to the blocker, and everything observed around
it. Local HEAD `357ffec` == origin/main (verified via ls-remote);
working tree clean.

---

## a) FULLY DONE (verifiably complete, committed, tested)

1. **M15 — self-send fast refusal (send-failure train C), both lanes.**
   `gateway.SelfSendRejection` (internal/gateway/selfsend.go, one home
   for the shared logic) refuses a message OR fax addressed to the
   extension's own DID (config `identities`) BEFORE the gateway call —
   but after the row/job persists, so the evidence a provider refusal
   would have left survives. The refusal is a locally synthesized
   `ErrProviderRejected{400}` → rides the existing train-E 422 arm
   (banner + toast with reason, no retry affordance). Guard off when
   unconfigured (nil map / no entry). Evidence: commit `37ffc53` (+ two
   daemon sweeps carrying the staged remainder); service tests ×2 lanes
   — provider NEVER consulted (counting gateway fake), row FAILED,
   kind "rejected", Rejection family via errorfamilytest, formatted-DID
   sanitize parity pinned; server tests — `TestThreadViewWarnsOnSelfSend`
   rewritten to the new behavior (422 + failed bubble + notice) and new
   `TestFaxToOwnNumberIsRefusedLocally`. Full `go test -count=1 ./...`
   green (raw exit 0, twice). Docs: error-contract row extended,
   send-failure plan doc carries the shipped-note with the
   both-halves resolution, TODO rows 39/41 updated (incl. the 2.7.0
   post-deploy expectation change).
2. **M18 — go-health `WithEvaluationHook` counters: built, found dead,
   PARKED with evidence.** The hook fires only inside `Probe.Evaluate`;
   this wiring serves readiness via cqrs-htmx (no second readiness
   truth) and runs go-health live (refresh 0) — liveness evaluates
   nothing, startupz never calls Evaluate (verified in BOTH v0.3.0 and
   v0.4.0 sources: identical). 134 lines wired, integration test
   failed, root-caused, reverted clean in `f7028b4`; TODO row 48 closed
   as parked with the full why + revisit triggers.
3. **M21 — branded-id seam comment.** The parked Valuer/Scanner
   adoption decision now lives at its one home: the `parseID` doc
   comment in internal/domain/ids.go (commit `003f4f5`/daemon `ff7031a`).
4. **M20 — registry/doc sync.** AGENTS: templ-components bullet
   rewritten to the post-spike reality (EmptyState ×6 adopted, permanent
   `/assets/tw.css` + rebuild recipe + v3-never warning, rejected waves
   with rationale, verdict-doc pointer); gateway-seam bullet gains the
   train-C refusal; middleware bullet gains the httpspec 19-spec pin.
   FEATURES rows 82 (own-number identity) and 159 (send-path guards)
   updated. Error-contract row updated (under M15).
5. **M22 — standing watches refreshed.** TODO row 44: templ-components
   latest is now v1.19.3 (tag-only, no release notes; adopted v1.19.2;
   trigger stays v1.20.x, patch rideable at next sweep), re-check date
   stamped. E2E-budget numbers deliberately deferred to the release
   E2E runs (see b).
6. **M19 pre-tail — release preparation, all green:**
   - Spike removed: `/dev/spike/tailwind` route, `views/spike.templ` +
     generated, `assets/spike/probe.js`, embed entry, both route
     registrations — zero "spike" references after (commit `b7ed8fb`,
     297 deletions). `/assets/tw.css` kept (permanent).
   - CHANGELOG folded: `## [2.7.0] - 2026-09-24` with the full train
     (train C, EmptyState/tw.css with the proven-coexistence story,
     bounded uploads, redaction, Retry-After, dep sweep, internal
     quality) — commit `78b30cf`.
   - `webphoneVersion` 2.6.0 → 2.7.0 in flake.nix — `5a689ef`.
   - HubFanOut benchmark re-run (MANDATORY: cqrs-htmx AND go-sse both
     bumped) with the new `-benchtime=1s -count=5` methodology: no
     regression, median table appended to the baseline doc — `aa29e47`.
   - Gates that don't need a working host-nix builder, run via STORE
     BINARIES (bypassing the broken `nix develop`): full go suite
     green (store go 1.27.1); **smoke 42 main + 4 restart checks, 0
     failed, `/version` exactly `v2.7.0`** (store-go build with the
     nix ldflags variable — first try failed on the wrong variable
     name, fixed and re-run); island `node:test` suite green (store
     node 26.8.1, exit 0).
7. **Owner-terminal command sheet** —
   `docs/planning/2026-09-24_19-25_owner-terminal-command-sheet.md`:
   the URGENT binfmt fix (root one-liner), pbx-artmann relock ritual
   (rev-parse discipline), prod deploy + post-deploy verification with
   the NEW train-C self-send expectation, SMS-bridge journal triage,
   owner-calls pointer, announcement approvals. **v2.7.0 announcement
   drafts** (one-liner / headline / short-note) at
   `docs/announcements/2026-09-24_v2-7-0_drafts.md` following the
   v2.6.0 pattern. Commit `357ffec`.
8. **Hygiene**: pushed everything (local == remote == `357ffec`,
   verified twice via ls-remote); no side effects in the sibling repos
   from the failed release attempt (stack/pbx trees verified clean);
   all releases-attempt logs preserved at /tmp/release-2.7.0-1.log,
   /tmp/smoke-270.log, /tmp/hub-bench.log.

## b) PARTIALLY DONE

1. **M19 — the v2.7.0 release train itself.** DONE: fold, bump, spike
   removal, benchmark, go suite, smoke, island tests (above). REMAINING,
   ALL blocked on the host-nix outage (see d1): `BUILDFLOW_NO_RESULT_CACHE=1
   buildflow` full (attempt 1 died on the outside-develop toolchain, see
   d2), `nix flake check` (NEVER run this session — incl. the KVM backup
   VM test and treefmt/statix/island-lint), vulnix over the runtime
   closure, annotated tag + push, lychee link check, **stack bump +
   browser E2E ×2 (mandatory — markup changed with the EmptyState
   swap)**, aarch64 cross-build + ELF `b700` verify, gh release with
   the CHANGELOG section. Resume command is ready and corrected:
   `nix develop -c scripts/release.sh 2.7.0 > /tmp/release-2.7.0-2.log 2>&1`
   (the develop-wrap fixes attempt 1's buildflow-env failure; the bump
   step is idempotent). Effort once unblocked: ~45–90 min (E2E ×2
   dominated), budget 445s/run.
2. **Owner sheet stack-hash placeholder.** The sheet's §1/§2 reference
   the new stack commit; the hash only exists after the release's
   stack-bump step. S-fill: 1 minute post-release.
3. **M22 tail — E2E budget watch numbers.** The watches row needs the
   fresh browser-E2E wall times from the release runs (rule: two
   consecutive over-budget runs). Cannot be honest numbers until the
   E2E actually runs.
4. **Post-release closing sweep** (runbook step 9): dead-process
   proofs, one no-cache buildflow (gitleaks/codespell via
   scripts/buildflow.sh), ls-remote verify — queued behind the tag.

## c) NOT STARTED (and why)

1. **Owner-terminal actions** — not assistant-startable by AGENTS
   (pbx forbids ssh/deploy; host root not mine): prod deploy (prod
   STILL serves v2.5.0 — two releases behind), pbx-artmann relock,
   live self-send verification, telnyx-webhooks journal triage (M3),
   owner-calls batch sitting (M16), announcement posting (M17 — drafts
   for v2.1.0 through v2.7.0 all waiting). All commands prepped in the
   sheet.
2. **Send-failure F (own-DID composer warning)** — deliberately not
   started: strictly conditional on self-send recurrence after B+C;
   no recurrence observed. Likely never needed.
3. **templ-components v1.19.3 patch ride** — deliberately deferred to
   the next dependency sweep (tag-only release, no notes; taking it
   mid-release-prep would burn a vendorHash roundtrip + re-verification
   for an undocumented patch).
4. **Root-causing the /run/binfmt disappearance** — I gathered evidence
   (kernel registration alive, symlink gone, timing coincides with the
   concurrent session's VM tests/host builds at load 32–46) but could
   not determine the actor; needs owner/host access.

## d) TOTALLY FUCKED UP

1. **THE HOST'S NIX IS DOWN — 9+ hours and counting.** `/run/binfmt`
   (the symlink nix probes for emulation) vanished ~19:10 on 2026-09-24
   and is STILL missing at 04:04. EVERY nix build on this host fails
   (`error: getting attributes of path "/run/binfmt": No such file or
   directory`) — `nix develop`, `nix build`, VM tests, everything; the
   kernel's binfmt_misc still has aarch64 registered, so it is purely
   the missing symlink. Severity: blocks the entire release tail (d)
   and any concurrent session's nix work; no data loss, product code
   unaffected. Workarounds exhausted: `extra-platforms` and `sandbox`
   nix options are RESTRICTED for untrusted users (warned + ignored,
   tried both), recreating `/run/binfmt` needs root (root-owned tmpfs),
   systemctl is banned for the assistant. Mitigation documented: `sudo
   systemctl restart systemd-binfmt.service` (owner sheet §0) — one
   line, not run in 9 hours (overnight; nobody alerted).
2. **Release attempt 1 wasted a full gate cycle on a scripted trap.**
   `scripts/release.sh` invokes bare `env BUILDFLOW_NO_RESULT_CACHE=1
   buildflow` — outside a develop shell that dies on the host go
   1.26.7 vs go.mod 1.27.1 floor (7 step failures; exactly the trap
   `.buildflow.yml`'s header documents for bare `buildflow`, which is
   why `scripts/buildflow.sh` exists as the self-healing wrapper —
   release.sh doesn't use it). My fault for launching the script
   outside develop; the script's fault for not guarding it. No side
   effects (failed at gates, before any tag/stack step — verified).
3. **Session fumbles (all caught and fixed in-flight, recorded for the
   pattern)**: (i) a multiedit replaced `func TestInboundFaxWebhook...`
   with a stray comment — repaired in the next edit before any test
   ran; (ii) my M15 first edit renamed `err`→`sendErr` and left three
   dangling references + a shadowing `sendErr :=` — caught by reading
   the diff before building, restructured to plain assignment; (iii)
   direct `oxfmt` invocation failed ("all matched files may have been
   excluded") — fell back to treefmt (correct owner anyway); (iv) `rg
   -rn` is not "recursive + line-numbers" (-r = REPLACE) — garbled the
   first go-health source read, redone correctly; (v) the commit
   heredoc attribution lines broke under mvdan/sh quoting (message
   bodies intact — content over ceremony); (vi) repeated edit-tool
   mtime races with the daemon on shared files — switched to
   python-keyed inserts, which never lost a race after.

## e) WHAT WE SHOULD IMPROVE

1. **release.sh should self-guard its toolchain** — swap the bare
   `buildflow` call for `scripts/buildflow.sh` (exists for exactly
   this) or abort with a clear message when not inside a develop shell.
   Cost of the current trap: one wasted gate cycle per naive launch.
   Effort S.
2. **release.sh (or buildflow preflight) should check
   `test -e /run/binfmt`** and abort with the one-line fix — turns a
   cryptic mid-gate nix death into an actionable message. This outage
   would have been caught at attempt-1 preflight instead of after a
   buildflow cycle. Effort S.
3. **Host monitoring for the binfmt symlink** — a single missing
   symlink silently killed all builds for 9+ hours overnight. A
   systemd-timer/path check (or a stack-runbook probe) should page.
   Effort S.
4. **Codify the store-binary fallback in AGENTS/lessons** — running the
   full go suite + smoke + island tests via store `go 1.27.1` /
   `nodejs 26.8.1` binaries kept this session moving during the outage;
   the pattern (locate `/nix/store/*-go-1.27*/bin/go`, set
   GOTOOLCHAIN=local, replicate nix ldflags for version-stamped builds)
   is reusable and is currently tribal knowledge from this session.
   Effort S.
5. **Finish reading a library seam's FIRING PATHS before building on
   it** — M18 cost ~20 min + a revert commit because the briefing's
   design assumed the hook fires; reading `handlers.go` (where
   StartupHandler calls `runHealthChecks` directly, never Evaluate)
   took two minutes afterwards. The verify-first reflex must bind
   BEFORE the first line of wiring, not after the integration test
   fails. (Personal process note; candidate for lessons.md.)
6. **Use python-keyed inserts for daemon-contended files from the
   start** — the edit tool's read-freshness check lost every race
   tonight on shared files (TODO_LIST, plan docs, pages.go); the
   python-anchor pattern never did. Zero-cost switch, next session.
7. **CHANGELOG house style** — the folded 2.7.0 section left an empty
   `## [Unreleased]` header directly above `## [2.7.0]` (standard
   keep-a-changelog, fine) but the Changed section carries a stray
   double blank line where bullets moved; cosmetic, prettier/treefmt
   don't own markdown — eyeball it at tag time.

## f) Next things (ranked; 1–6 are the release tail, then owner items, then backlog)

| # | Task | Impact | Effort | Category |
|---|------|--------|--------|----------|
| 1 | OWNER: `systemctl restart systemd-binfmt.service`, confirm `/run/binfmt` back | Critical (unblocks all nix) | S | Ops |
| 2 | Resume release: `nix develop -c scripts/release.sh 2.7.0 > /tmp/release-2.7.0-2.log 2>&1` | Critical | M | Release |
| 3 | Release E2E ×2 green → record wall times into the watches row (budget 445s) | Critical | S | Release |
| 4 | aarch64 cross-build + ELF `b700` byte verify (release step 8) | High | S | Release |
| 5 | gh release v2.7.0 object (CHANGELOG section as notes); link check via lychee | High | S | Release |
| 6 | Post-release closing sweep: no-cache buildflow once, ls-remote verify, dead-process proofs | High | S | Release |
| 7 | Fill the stack hash into owner sheet §1/§2; close the release TODO rows | High | S | Docs |
| 8 | OWNER: pbx-artmann relock ritual (sheet §2) | High | M | Ops |
| 9 | OWNER: deploy chain to prod (still v2.5.0!) + smoke `--expect-version 2.7.0` | High | M | Ops |
| 10 | OWNER: live self-send check — NEW expectation: local 422 refusal + failed row | High | S | Ops |
| 11 | OWNER: telnyx-webhooks journal triage (prod SMS bridge failure, M3) | High | S | Ops |
| 12 | OWNER: v2.7.0 (+backlog v2.1.0–v2.6.0) announcement approvals | Medium | S | Comms |
| 13 | OWNER: owner-calls batch sitting (M16 — unblocks parked policy items) | High | M | Decisions |
| 14 | Root-cause the /run/binfmt disappearance; add the release.sh/host preflight (e1–e3) | High | S | Hardening |
| 15 | templ-components v1.19.3 patch ride at next dep sweep (vendorHash same-breath) | Low | S | Deps |
| 16 | tw.css regeneration whenever templ-components bumps (recipe in verdict doc) | Low | S | Deps |
| 17 | erraudit tier-2 re-measure due 2026-10-22 (baseline 127/113, must shrink) | Medium | S | Quality |
| 18 | Quarterly watches re-check due 2026-12-20 | Low | S | Hygiene |
| 19 | AGENTS/lessons: codify store-binary fallback pattern (e4) | Medium | S | Docs |
| 20 | release.sh hardening PR (develop-guard + binfmt preflight, e1+e2) | Medium | S | Tooling |
| 21 | Deploy-ordering decision: straight to 2.7.0 vs 2.6.0 first (prod on 2.5.0) | Medium | S | Decisions |
| 22 | Verify gh release + tag links resolve (v2.6.0 ritual) after publish | Medium | S | Release |
| 23 | CHANGELOG 2.7.0 compare-link pass at tag time (cosmetic, e7) | Low | S | Docs |
| 24 | Send-failure F — keep NOT building it unless self-sends recur | Low | — | Feature |
| 25 | Post-release: re-grep "spike" at the tag to prove the release tree is spike-free | Low | S | Release |
| 26 | If v1.20.x lands: re-evaluate RelativeTime only if it gains server-render mode | Low | M | Deps |

## g) Questions I cannot answer myself

1. **Can you restore host nix?** `sudo systemctl restart
   systemd-binfmt.service` (then `ls -la /run/binfmt` to confirm). It
   has been down since ~19:10 yesterday; every nix build on this host
   fails; I've exhausted the unprivileged workarounds (restricted nix
   options, no root writes to /run). Everything in f1–f6 waits on this.
2. **Do you know what removed `/run/binfmt`?** The kernel's aarch64
   registration survived — only the symlink died, coincident with the
   concurrent session's VM tests / host toplevel builds (host load was
   32–46 at the time). If you know the actor (a `nixos-rebuild`? a
   unit restart?), f14's prevention gets a precise fix instead of a
   generic preflight.
3. **Deploy ordering once 2.7.0 is tagged:** prod serves v2.5.0 today —
   go straight to 2.7.0, or deploy 2.6.0 first as a stepping stone?
   Owner call (it decides which smoke expectations each post-deploy
   check uses; the sheet currently assumes straight-to-2.7.0).

---

*Point-in-time snapshot. Section (f) is the harvest input for
TODO_LIST/ROADMAP upkeep. Report written while the release tail waits
on g1; resume command is one paste away.*
